[package]
name = "voice-patch-studio"
version = "0.1.0"
description = "Local voice cloning studio for editing patch audio"
authors = ["Voice Patch Studio"]
license = "UNLICENSED"
repository = ""
edition = "2021"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
